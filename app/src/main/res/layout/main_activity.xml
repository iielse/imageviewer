<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <Button
        android:id="@+id/orientation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Horizontal"
        android:textAllCaps="false"
        android:textSize="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/fullScreen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="FullScreen(off)"
        android:textAllCaps="false"
        android:textSize="10dp"
        app:layout_constraintBottom_toBottomOf="@id/orientation"
        app:layout_constraintStart_toEndOf="@id/orientation" />

    <Button
        android:id="@+id/loadAllAtOnce"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="LoadAllAtOnce(off)"
        android:textAllCaps="false"
        android:textSize="10dp"
        app:layout_constraintBottom_toBottomOf="@id/orientation"
        app:layout_constraintStart_toEndOf="@id/fullScreen" />

    <Button
        android:id="@+id/simplePlayVideo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Video(simple)"
        android:textAllCaps="false"
        android:textSize="10dp"
        app:layout_constraintBottom_toTopOf="@id/orientation"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/customTransition"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="customTransition"
        android:textAllCaps="false"
        android:textSize="10dp"
        app:layout_constraintBottom_toBottomOf="@id/simplePlayVideo"
        app:layout_constraintStart_toEndOf="@id/simplePlayVideo" />
</androidx.constraintlayout.widget.ConstraintLayout>